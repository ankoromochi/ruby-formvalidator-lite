require 'spec_helper'

describe Formvalidator::Lite do
  it 'has a version number' do
    expect(Formvalidator::Lite::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
